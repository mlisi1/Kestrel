# Core
torch
numpy
matplotlib
plyfile

# Qt viewer
PyQt5

# Rendering core (model loading, camera, culling/LOD, compression, profiling,
# SH eval, depth-to-normal) + its vendored CUDA rasterizer — both git
# submodules of this repo, installed editable:
# pip install -e gsplat2d-rendering
# pip install -e gsplat2d-rendering/third_party/diff-surfel-rasterization
